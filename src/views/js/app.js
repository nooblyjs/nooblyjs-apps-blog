
/**
 * @fileoverview Updated Blog Application 
 * 
 * @author NooblyJS Team
 * @version 0.0.0
 * @since 0.0.0
 */
class BlogApp {

    constructor() {        
        this.init();
    }

    init() {

    }
}

// Initialize the application when the DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    window.wikiApp = new BlogApp();
});